spring:
  datasource:
    url: jdbc:h2:mem:testdb
    data: dataTest.sql
  h2:
    console:
      enabled: true

  jpa:
    generate-ddl: false
    hibernate:
      ddl-auto: validate

    show-sql: true
    properties:
      hibernate:
      format_sql: true